@model MeetingApp.Models.IndexModel
@using PagedList.Mvc;
@using PagedList;
@{
    ViewBag.Title = "Home Page";
}
@section Scripts{
    <script src="~/Scripts/jquery.unobtrusive-ajax.min.js"></script>

    <script type="text/javascript">

        function ClearMeetingDetails() {
            $('#meetingDetails').replaceWith(' <div id="meetingDetails"></div> ');
        }

    </script>
}
    

    <div class="container">
       
        <div class="row">
            <div class="col-md-8">
                @if (User.IsInRole("LA"))
                {


                    <img src="~/Images/WPG_Picture.png" width="2500" height="2500" class="img-responsive" />
                }

                @if (User.IsInRole("A"))
                {


                    <img src="~/Images/Admin and Executive Admin.jpg" width="2500" height="2500" class="img-responsive" />
                }


                @if (User.IsInRole("E"))
                {

                    <img src="~/Images/WPG_Picture.png" width="2500" height="2500" class="img-responsive" />

                }


                @if (User.IsInRole("BE"))
                {

                    <img src="~/Images/Admin and Executive Admin.jpg" width="2500" height="2500" class="img-responsive" />


                }
                <hr />
                <div class="col-lg-12">
                    <h4>Guest Info</h4>
                    @foreach (var guest in Model.Guests)
                    {
                        <p class="alert alert-info"><i class="glyphicon glyphicon-user"></i> Name: @guest.L_Name, @guest.F_Name    &nbsp;  &nbsp; <i class="glyphicon glyphicon-home"></i> Company:@guest.Company.Name</p>
                    }
                </div>
            </div>
            <div class="col-md-4">

                @*<h4>Employee Status</h4>
                @Model.Employee.EmployeeStatus*@
                <hr />
                @Html.Partial("EmployeeMeetings", Model.Meetings)

                <div class="row">
                    <div class="col-lg-12">
                        <div id="meetingDetails">

                        </div>
                    </div>
                </div>
            </div>



            <div class="col-md-12" style="margin-top: 20px;">
                <hr style="color: red" />
                <div class="row">
                    @*@foreach (var note in Model.Notes.Take(3))
                    {
                        <div class="col-md-4">
                            <div class="alert alert-dismissible alert-success">
                                <p>Type: @note.NoteType </p>
                                <p>Date: @note.Date </p>
                                p>Employee: @note.Employee.L_Name, @note.Employee.L_Name </p>
                                <p>Description: @note.Description </p>
                            </div>
                        </div>
                    }*@


                </div>
            </div>
        </div>


    </div>
